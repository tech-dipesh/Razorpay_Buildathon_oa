{
  "engines": {
    "node": ">=24.0.0"
  },
  "packageManager": "pnpm@11.24.0",
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "test": "turbo run test",
    "lint": "turbo run lint",
    "type": "turbo run type",
    "generate": "turbo run generate",
    "seed": "turbo run seed",
    "clean": "turbo run clean"
  },
  "devDependencies": {
    "turbo": "^2.10.4",
    "typescript": "^6.0.3"
  }
}
